var React = require('react');

var PhotoUploadModal = React.createClass({
  getInitialState: function () {
    return { sshowModal: false };
  },

  render: function () {
    return (
      <div>
      modal.
      </div>
    );
  }
});

module.exports = PhotoUploadModal;
